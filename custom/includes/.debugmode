# debug Startup Configuration

# Allows debugging of this startup configuration by enabling the following if enabled:
# - Turns on showing notice messages
# - Prompt for enter to continue before tmux fires (which hides startup messages)

if [[ $ZSH_DEBUG_MODE ]]; then
  # Turn on notice message
  export ZSH_FORCE_NOTICES=true
fi